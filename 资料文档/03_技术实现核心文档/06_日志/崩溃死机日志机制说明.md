# Coffee2 崩溃死机日志机制说明

> 状态标签：CONFIRMED / PARTIAL / NOT_IMPLEMENTED / UNKNOWN / OUT_OF_SCOPE / HISTORICAL
> 编写日期：2026-09-01
> 说明：本文只记录**当前源码实际存在**的崩溃机制，不将理想设计写成已实现功能。

---

## 1. 异常入口

CONFIRMED（源码 `Application/Common/Diagnostics` + `CubeMX_Base/Core/Inc/FreeRTOSConfig.h` 的 hook）。

| 入口 | 状态 | 说明 |
| --- | --- | --- |
| HardFault_Handler | PARTIAL | 由 `app_crash_fault_*.S` 汇编包装接管（ARMCC / GCC 各一份） |
| MemManage_Handler | PARTIAL | 同上 |
| BusFault_Handler | PARTIAL | 同上 |
| UsageFault_Handler | PARTIAL | 同上 |
| NMI_Handler | NOT_IMPLEMENTED | 未单独接管（UNKNOWN，需核） |
| assert_failed | PARTIAL | 通过 `configASSERT` → `vAppCrashDiagAssertCEntry/AssertEntry` |
| FreeRTOS stack overflow hook | CONFIRMED | `configCHECK_FOR_STACK_OVERFLOW=2` + `traceTASK_CREATE` 登记 |
| malloc failed hook | NOT_IMPLEMENTED | 未发现 `vApplicationMallocFailedHook` 强实现（需核） |
| configASSERT | CONFIRMED | `FreeRTOSConfig.h` 中 `vAppCrashDiagAssertCEntry` |
| watchdog reset | PARTIAL | `vAppCrashDiagWatchdogRefresh` 存在，默认 `APP_CRASH_IWDG_REFRESH_ENABLE=0` |
| brownout / software reset | UNKNOWN | 未核 `prvCaptureResetCause` 相关（`coffee2_manager.c` 有 reset cause 捕获） |

---

## 2. 崩溃上下文逐项状态

CONFIRMED（来自 `app_crash_diag.c`、`app_crash_diag.h`、汇编包装）：

| 项 | 状态 | 说明 |
| --- | --- | --- |
| R0-R3 | IMPLEMENTED | 异常帧解析（`prvBuildFaultText` 从硬件栈帧取） |
| R12 | IMPLEMENTED | 同异常帧 |
| LR | IMPLEMENTED | 帧/寄存器输出 |
| PC | IMPLEMENTED | 帧/寄存器输出 |
| xPSR | IMPLEMENTED | 帧/寄存器输出 |
| MSP/PSP | IMPLEMENTED | `g_aulAppCrashSavedRegisters[10]` 保存 MSP/PSP |
| EXC_RETURN | IMPLEMENTED | `vAppCrashDiagFaultEntry(..., ulExcReturn, ...)` |
| R4-R11 | IMPLEMENTED | ARMCC 汇编包装保存（`g_aulAppCrashSavedRegisters`） |
| CFSR/HFSR/DFSR/AFSR | IMPLEMENTED | `prvAppendScbRegisters` |
| MMFAR/BFAR | IMPLEMENTED | 同上 |
| 当前任务名 | IMPLEMENTED | 任务登记表 `s_axCrashTasks` 或 `g_pvAppCrashCurrentTask` |
| 当前任务句柄 | IMPLEMENTED | 同上 |
| 当前中断号 | UNKNOWN | 未核是否输出 ICSR/中断号 |
| 最近一次日志 | NOT_IMPLEMENTED | 崩溃不走日志 ring，不携带最近日志 |
| 最近一次命令/设备/总线 | NOT_IMPLEMENTED | 公共核心无此信息；Coffee2 未在崩溃前快照（UNKNOWN） |
| 当前寄存器地址 | IMPLEMENTED | SCB 故障寄存器地址范围 |
| 复位原因 | PARTIAL | `coffee2_manager.c` 有 `prvCaptureResetCause`，但崩溃报告不包含 |

---

## 3. 崩溃输出路径

CONFIRMED。

- **路径**：崩溃文本冻结 → `lAppCrashDiagWrite()` → Coffee2 强绑定 `lAppCommLogPortCrashWrite()`（`coffee2_crash_log_port.c:18`）→ **LL-USART 轮询 huart1**。
- **依赖调度**：否。崩溃路径调度器无关，不依赖任务。
- **是否可能在 HardFault 中再次异常**：有防护——`s_ucCrashActive` 防重入、任务栈范围校验、UART 自旋 `APP_CRASH_UART_SPIN_LIMIT`。
- **是否依赖 DMA/中断**：否。崩溃前 `CLEAR_BIT(CR3, DMAT)` 清 DMA 传输，轮询 TXE/TC。
- **是否可能卡住看门狗**：`vAppCrashDiagWatchdogRefresh()` 默认关闭（`APP_CRASH_IWDG_REFRESH_ENABLE=0`）。若实际使能则会在崩溃循环刷新 IWDG。
- **下次启动读取上次崩溃**：NOT_IMPLEMENTED。崩溃文本只在当前崩溃现场重复输出，不写入 Flash/Backup SRAM（UNKNOWN 需核）。
- **固定长度/校验/版本**：文本缓冲 `APP_CRASH_TEXT_BUFFER_SIZE=4096`、报告含 `CRASH_*` 标记与重复周期 `REPEAT_SECONDS`；无显式校验字段（UNKNOWN）。

---

## 4. 崩溃后的系统行为

CONFIRMED（`app_crash_diag.c` 的 `prvCrashRun` 逻辑）。

| 行为 | 状态 |
| --- | --- |
| 自动复位 | NOT_IMPLEMENTED（崩溃后进入重复输出循环，不主动复位） |
| 停机 | PARTIAL（在崩溃循环内重复输出冻结文本） |
| 等待调试器 | UNKNOWN（未核 BKPT/调试停机） |
| 关闭危险输出 | UNKNOWN（未核崩溃前的安全关输出） |
| 保存复位原因 | PARTIAL（manager 捕获，但不写入崩溃报告） |
| 下次启动打印 | NOT_IMPLEMENTED（无持久存储） |
| 清除旧记录 | PARTIAL（每次崩溃重建冻结文本） |
| 重复崩溃循环 | 设计为 10s 重复输出，等同卡在崩溃现场（CONFIRMED） |

---

## 5. Coffee2 使用方式

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 接入崩溃接口 | CONFIRMED | `coffee2_crash_log_port.c` 提供强 `lAppCrashDiagWrite` |
| 初始化位置 | CONFIRMED | `vAppCrashDiagInit()` 在业务初始化调用；`traceTASK_CREATE` 在 `FreeRTOSConfig.h` |
| 异常入口 | CONFIRMED | 汇编包装 `app_crash_fault_*.S` |
| 保存设备/总线信息 | NOT_IMPLEMENTED | 公共核心无；Coffee2 未在崩溃时快照设备/总线 |
| 保存 Workflow step | NOT_IMPLEMENTED | 同上 |
| 保存 Robot action | NOT_IMPLEMENTED | 同上 |
| 保存 Modbus 地址/功能码 | NOT_IMPLEMENTED | 同上 |
| 启动读取上次崩溃 | NOT_IMPLEMENTED | 无持久化 |
| 推荐接入点 | 建议 | 在崩溃入口捕获 Coffee2 私有上下文（workflow step、当前设备/总线、robot action）作为建议扩展 |

---

## 6. 其他 target 状态

| Target | 状态 |
| --- | --- |
| Coffee2 | **INTEGRATED**（强绑定崩溃输出 + 日志适配完整） |
| Coffee3 | UNKNOWN（未在本次调研中确认是否物理存在） |
| MilkTea | **OUT_OF_SCOPE** —— 本次不以 MilkTea 源码或日志实现作为当前 Coffee2 架构依据；其旧崩溃机制文档（`死机日志管理与排障说明.md`）标记 `HISTORICAL` |

---

*结束。所有"已实现/未实现/未知"均基于当前源码与配置；硬件实机行为标注 UNKNOWN。*
