# Legacy self-developed Modbus implementation

This directory is retained only for source comparison and historical review.
It is not part of the active Keil target and must not receive new product
dependencies.

The active implementation is:

- `Application/ProtocolStack/ModbusPort`
- `Middlewares/New_Party/nanoMODBUS`

Do not delete this directory unless the project owner explicitly retires the
reference copy.

